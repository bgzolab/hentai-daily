name: sync

on:
  workflow_dispatch:
  schedule:
    # https://github.blog/changelog/2026-03-19-github-actions-late-march-2026-updates/?ref=dailydev
    # https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#onschedule
    - cron: "0 */2 * * *"
      timezone: "Asia/Singapore"
    - cron: "1 0 * * *"
      timezone: "Asia/Singapore"
  push:
    branches:
      # 开发分支验证路由
      - "vitepress-dev"

jobs:
  sync:
    name: Fetch items and update JSON files
    runs-on: ubuntu-latest
    permissions:
      contents: write # 需要允许 git push 回仓库
    steps:
      - name: Set TimeZone
        uses: szenius/set-timezone@v1.2
        with:
          timezoneLinux: "Asia/Singapore"
          timezoneMacos: "Asia/Singapore"
          timezoneWindows: "Singapore Standard Time"

      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.13.2

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r src/requirements.txt
        if: steps.pip-cache.outputs.cache-hit != 'true'

      - name: Ensure jq is available
        run: |
          if ! command -v jq &> /dev/null; then
            echo "jq not found, installing..."
            sudo apt-get update && sudo apt-get install -y jq
          else
            echo "jq is available: $(jq --version)"
          fi

      - name: Run Script
        run: |
          python3 src/sync.py
          python3 src/heatmap-count.py

      - name: Format JSON with jq
        run: |
          # 遍历所有 JSON 文件并用 jq 格式化（2 个空格缩进）
          # 使用 -print0 和 read -d '' 安全处理文件名
          find api/archives -maxdepth 4 -name "*.json" -type f -print0 | \
            while IFS= read -r -d '' file; do
              if jq --indent 2 '.' "$file" > /tmp/jq-tmp 2>/dev/null; then
                mv -f /tmp/jq-tmp "$file"
                echo "✓ Formatted: $file"
              else
                echo "✗ Skipped (invalid JSON): $file"
                rm -f /tmp/jq-tmp
              fi
            done

      - name: Run Safe Dir
        run: |
          git config --global --add safe.directory /github/workspace

      - name: Push Articles
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add .
            git commit -m "Sync feed"
            git push
          else
            echo "no changes";
          fi
